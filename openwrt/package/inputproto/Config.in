config BR2_PACKAGE_INPUTPROTO
        prompt "inputproto..........................X input protocol extension"
	tristate
	default n
	help
	  X input protocol extensions. You probably only need this if you
	  want to install an X server.
