config BR2_PACKAGE_LIBESTR
	prompt "libestr........................... some essentials for string handling (and a bit more)"
	tristate
	default m if CONFIG_DEVEL
	help
          Some essentials for string handling (and a bit more)
