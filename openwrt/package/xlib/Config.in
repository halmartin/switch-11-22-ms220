config BR2_PACKAGE_XLIB
        prompt "xlib................................Xlib library"
	tristate
	default n
	help
	  Xlib library. You probably only need this if you want to
	  install an X server.
