config BR2_PACKAGE_XF86_INPUT_MOUSE
	prompt "xf86-input-mouse................... X server mouse input driver"
	tristate
	default m if CONFIG_DEVEL
	help
	  X window server mouse driver. You probably only need this
	  if you are building an X server.
